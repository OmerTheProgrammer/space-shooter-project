<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<PropertyGroup>
		<IncludeCompositeObjects>True</IncludeCompositeObjects>
		<TargetDatabaseName>Space_Shooter_DB</TargetDatabaseName>
		<DeployScriptFileName>Space_Shooter_DB Creator.sql</DeployScriptFileName>
		<TargetConnectionString>Data Source=(localdb)\MSSQLLocalDB;Integrated Security=True;Persist Security Info=False;Pooling=False;Multiple Active Result Sets=False;Connect Timeout=60;Encrypt=True;Trust Server Certificate=True;Command Timeout=0</TargetConnectionString>
		<ProfileVersionNumber>1</ProfileVersionNumber>

		<!-- CRITICAL FIXES START HERE -->

		<!-- Stop CREATE DATABASE command (Fixes Msg 5170) -->
		<CreateDatabase>False</CreateDatabase>

		<!-- Stop DROP/RECREATE DATABASE command (Fixes accidental data loss) -->
		<CreateNewDatabase>False</CreateNewDatabase>

		<!-- Allow column drops/table rebuilds to proceed (Bypasses the data loss check) -->
		<AllowTableRecreation>True</AllowTableRecreation>

		<!-- Keep the default behavior to block if data loss *might* occur (safety), 
         but the above flag overrides it for the column drop. -->
		<BlockOnPossibleDataLoss>True</BlockOnPossibleDataLoss>

	</PropertyGroup>
</Project>
